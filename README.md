# 🎯 Sales Best Friend - AI Sales Coach

Real-time AI coaching for sales calls with smart guidance, progress tracking, and client insights.

## 🎉 New Interface!

Complete dashboard redesign with:
- **Next Step Recommendations** - AI-powered guidance for each stage
- **Client Information Tracking** - Auto-detected objections, interests, needs
- **Progress Checklist** - Track your call against best practices
- **Stage Detection** - Know where you are (Greeting → Discovery → Presentation → Objections → Closing)

👉 **[See New Interface Documentation → NEW_INTERFACE.md](./NEW_INTERFACE.md)**

## 🏗️ Stack

- **Frontend**: React + Vite + TypeScript
- **Backend**: FastAPI + WebSockets (Python 3.11+)
- **LLM**: OpenRouter (anthropic/claude-3.5-sonnet) с JSON mode
- **ASR**: ✅ Faster-Whisper (real-time транскрипция!)

## 🎉 Новинка: Real-Time Transcription!

Теперь приложение **транскрибирует аудио в реальном времени** через Whisper!

- ✅ Захватите **любую вкладку** Chrome (YouTube, Google Meet, Zoom)
- ✅ Получайте **транскрипцию каждые 10 секунд**
- ✅ **Client Insights** обновляются автоматически
- ✅ **Подсказки от Claude** по ходу разговора

👉 **[Подробная инструкция → REALTIME_TRANSCRIPTION.md](./REALTIME_TRANSCRIPTION.md)**

## 📁 Структура проекта

```
SalesBestFriend/
├── backend/
│   ├── main.py              # FastAPI приложение
│   ├── requirements.txt     # Python зависимости
│   └── env.example          # Пример конфигурации
├── frontend/
│   ├── src/
│   │   ├── App.tsx          # Основной компонент
│   │   ├── App.css          # Стили
│   │   └── main.tsx         # Entry point
│   ├── package.json         # npm зависимости
│   └── env.example          # Пример конфигурации
└── README.md
```

## 🚀 Быстрый старт

### 1. Backend

```bash
# Перейти в директорию backend
cd backend

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Скопировать и настроить .env
cp env.example .env
# Отредактировать .env и добавить свой OPENROUTER_API_KEY

# Установить FFmpeg (для real-time транскрипции)
brew install ffmpeg  # macOS
# apt install ffmpeg  # Ubuntu/Debian
# choco install ffmpeg  # Windows

# Запустить сервер
uvicorn main:app --reload --port 8000
```

Backend будет доступен на `http://localhost:8000`

### 2. Frontend

```bash
# Перейти в директорию frontend
cd frontend

# Установить зависимости
npm install

# Скопировать и настроить .env
cp env.example .env
# Убедиться, что VITE_API_WS=ws://localhost:8000

# Запустить dev-сервер
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

## 🔑 Получение OpenRouter API Key

1. Перейти на [https://openrouter.ai/keys](https://openrouter.ai/keys)
2. Зарегистрироваться или войти
3. Создать новый API ключ
4. Скопировать ключ в `backend/.env`:
   ```
   OPENROUTER_API_KEY=sk-or-v1-...
   ```

## 🎮 Три режима работы

### 1. 🎤 Live Recording (Real-time)
- Захват любой вкладки Chrome (YouTube, Meet, Zoom)
- **Транскрипция каждые 10 секунд** через Whisper
- Подсказки и анализ в реальном времени
- **[Инструкция → REALTIME_TRANSCRIPTION.md](./REALTIME_TRANSCRIPTION.md)**

### 2. 📺 YouTube URL
- Вставьте ссылку на видео со звонком
- Полная транскрипция всего ролика
- Быстрый анализ готового контента
- **[Инструкция → YOUTUBE_READY.md](./YOUTUBE_READY.md)**

### 3. 📝 Text Mode
- Вставьте готовый текст разговора
- Мгновенный анализ без транскрипции
- Идеально для отладки и тестов
- **[Инструкция → DEBUG_MODE.md](./DEBUG_MODE.md)**

## 🎯 Быстрый старт (Live Mode)

1. Откройте frontend в браузере (`http://localhost:3000`)
2. Нажмите **"🎤 Начать запись"**
3. В диалоге выбора источника аудио выберите вкладку с Google Meet/YouTube
4. Убедитесь, что выбран чекбокс **"Share audio"** ⚠️
5. В реальном времени получайте:
   - **Транскрипцию разговора** (каждые 10 сек)
   - **Подсказки от AI коуча**
   - **Client Insights** (возражения, эмоции, интересы)
   - **Вероятность успеха сделки** (0-100%)

## 🔌 API Endpoints

### WebSocket: `/ingest`

Принимает аудио-чанки в формате `audio/webm;codecs=opus`

```typescript
const ingest = new WebSocket('ws://localhost:8000/ingest');
ingest.send(audioBlob); // Отправка бинарных данных
```

### WebSocket: `/coach`

Отправляет подсказки и probability score

```typescript
const coach = new WebSocket('ws://localhost:8000/coach');
coach.onmessage = (e) => {
  const { hint, prob } = JSON.parse(e.data);
  // hint: строка с подсказкой
  // prob: число от 0 до 1
};
```

### HTTP: `GET /`

Информация о сервисе

### HTTP: `GET /health`

Health check и статус подключений

## ⚙️ Конфигурация

### Backend (`backend/.env`)

```env
OPENROUTER_API_KEY=sk-or-v1-...
```

### Frontend (`frontend/.env`)

```env
VITE_API_WS=ws://localhost:8000
```

## 🧪 Текущие ограничения (MVP)

- ~~ASR (распознавание речи) пока **замоккан**~~ ✅ **ГОТОВО! Real-time Whisper!**
- Нет персистентности данных (всё в памяти)
- Нет аутентификации
- Один пользователь/сессия (можно расширить)
- Real-time транскрипция работает каждые 10 сек (не мгновенно)

## 🛠️ Roadmap

- [✅] Интеграция faster-whisper для реального ASR
- [✅] Real-time транскрипция
- [✅] YouTube поддержка
- [✅] Client Insights панель
- [ ] Сохранение истории звонков
- [ ] Мультипользовательский режим
- [ ] Dashboard с аналитикой
- [ ] Кастомизация промптов для коуча
- [ ] Экспорт транскриптов и инсайтов

## 🐛 Troubleshooting

### ❌ Не работает захват аудио

**Самая частая проблема!** Подробное решение: [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

**Краткий чеклист:**
- ✅ Используйте **Chrome/Edge/Brave** (не Firefox/Safari!)
- ✅ Выберите **"Chrome Tab"** (не весь экран)
- ✅ Выберите вкладку с **Google Meet**
- ✅ ⚠️ **ВКЛЮЧИТЕ галочку "Share audio"** — это критично!
- ✅ Нажмите **"Share"** (не Cancel)

Откройте консоль браузера (F12) для детальной диагностики.

### Backend не стартует

```bash
# Проверьте версию Python
python3 --version  # Должна быть 3.11+

# Переустановите зависимости
pip install --upgrade pip
pip install -r requirements.txt

# Установите FFmpeg (для real-time транскрипции)
brew install ffmpeg  # macOS
# apt install ffmpeg  # Ubuntu/Debian
# choco install ffmpeg  # Windows
```

### Frontend не подключается к WebSocket

1. Проверьте, что backend запущен на порту 8000
2. Убедитесь, что в `frontend/.env` указан правильный `VITE_API_WS`
3. После изменения `.env` перезапустите `npm run dev`

### OpenRouter ошибки

- Проверьте баланс API ключа на [openrouter.ai](https://openrouter.ai/)
- Убедитесь, что ключ скопирован полностью в `.env`
- Проверьте логи backend — он должен показать детали ошибки

## 📝 Лицензия

MIT

## 👥 Команда

Разработано для повышения эффективности продаж через AI-коучинг в реальном времени.

---

**Удачных продаж! 🚀**

