# 🔧 Решение проблем

## ❌ Ошибка захвата аудио

### Проблема: "Не удалось захватить аудио"

Эта ошибка возникает, когда браузер не может получить доступ к аудио из выбранной вкладки.

### ✅ Решение по шагам:

#### 1. Проверьте браузер
- ✅ **Chrome** (рекомендуется)
- ✅ **Microsoft Edge**
- ✅ **Brave**
- ❌ **Firefox** — НЕ поддерживает захват аудио из вкладок
- ❌ **Safari** — НЕ поддерживает захват аудио из вкладок

#### 2. При запросе разрешения:

Когда вы нажимаете "🎤 Начать запись", появится диалог выбора источника:

**ВАЖНО! Выполните ВСЕ эти шаги:**

1. Выберите вкладку **"Chrome Tab"** / **"Вкладка Chrome"** (не "Entire Screen" / "Весь экран")
2. Выберите именно ту вкладку, где открыт **Google Meet**
3. ⚠️ **КРИТИЧНО:** Поставьте галочку **"Share audio"** / **"Поделиться аудио"** внизу диалога
4. Нажмите **"Share"** / **"Поделиться"**

### 📸 Визуальная инструкция:

```
┌─────────────────────────────────────────┐
│  Choose what to share                   │
├─────────────────────────────────────────┤
│  ○ Entire Screen                        │
│  ● Chrome Tab         ← ВЫБЕРИТЕ ЭТО!  │
│  ○ Window                               │
├─────────────────────────────────────────┤
│  [Превью вкладки с Google Meet]         │
│                                         │
│  ☑ Share audio        ← ВКЛЮЧИТЕ!!!    │
│                                         │
│         [Cancel]    [Share]             │
└─────────────────────────────────────────┘
```

## 🔴 Типичные ошибки:

### NotAllowedError
**Причина:** Вы нажали "Cancel" или отклонили доступ

**Решение:**
1. Обновите страницу (F5)
2. Нажмите "Начать запись" снова
3. На этот раз нажмите "Share"

### NotFoundError
**Причина:** Не выбрана опция "Share audio"

**Решение:**
1. Попробуйте снова
2. Убедитесь, что галочка "Share audio" **ВКЛЮЧЕНА**

### NotSupportedError
**Причина:** Неподдерживаемый браузер

**Решение:**
Установите и используйте Google Chrome

## 🎯 Как проверить, что аудио захватывается:

После успешного захвата вы увидите:

1. ✅ Статус сменится на **"Подключено"** (зелёный)
2. ✅ В консоли браузера (F12) появятся сообщения:
   ```
   ✅ Аудио-поток получен: {tracks: 1, settings: {...}}
   🎙️ Запись началась
   📤 Отправлен chunk: 2048 bytes
   ```
3. ✅ В Chrome появится индикатор, что вы делитесь вкладкой

## 🔍 Дополнительная диагностика:

### Откройте консоль браузера (F12)

При успешном захвате вы должны увидеть:
```
🎤 Запрос захвата аудио...
✅ Аудио-поток получен: Object { tracks: 1, settings: {...} }
✅ /coach подключен
✅ /ingest подключен
🎙️ Запись началась
📤 Отправлен chunk: 2048 bytes
📤 Отправлен chunk: 1956 bytes
...
```

### При ошибке вы увидите:
```
❌ Ошибка старта записи: NotAllowedError: Permission denied
```

## 💡 Полезные советы:

1. **Сначала откройте Google Meet** в отдельной вкладке, затем запускайте захват
2. **Убедитесь, что в Meet идёт звонок** (иначе аудио может быть пустым)
3. **Не закрывайте вкладку Meet** во время записи
4. **Проверьте разрешения сайта** в Chrome: Settings → Privacy and security → Site settings → localhost

## 🆘 Всё ещё не работает?

1. Перезапустите браузер
2. Проверьте, что backend запущен: http://localhost:8000/health
3. Проверьте, что frontend подключается к правильному WebSocket: `VITE_API_WS=ws://localhost:8000` в `.env`
4. Попробуйте другой браузер (Chrome)

## 📱 Проверка работы backend:

```bash
# В терминале проверьте, что backend работает:
curl http://localhost:8000/health

# Должен вернуть:
# {"status":"ok","coach_connections":0,"last_hint":"","last_prob":0.0}
```

---

Если проблема сохраняется, проверьте консоль браузера (F12 → Console) и отправьте текст ошибки.

